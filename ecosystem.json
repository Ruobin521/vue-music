{
    "apps": [{
        "name": "vue-music-server",
        "script": "./server/app.js"
    }],
    "deploy": {
        "production": {
            "user": "dang",
            "host": ["39.106.124.95"],
            "port": "40000",
            "ref": "origin/master",
            "repo": "git@github.com:Ruobin521/vue-music.git",
            "path": "/www/vue-music/production",
            "ssh_options": "StrictHostKeyChecking=no",
            "post-deploy": "cd server;npm install;cd client;npm install;cd ../client;node client/build/build.js;cd ../&pm2 startOrRestart ecosystem.json  --env production",
            "env": {
                "NODE_ENV": "production"
            }
        }
    }
}